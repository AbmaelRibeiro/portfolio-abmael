# Portfolio Web App Layout

This is a code bundle for Portfolio Web App Layout. The original project is available at https://www.figma.com/design/xfXQOootaeMloDCo1zdhFz/Portfolio-Web-App-Layout.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
